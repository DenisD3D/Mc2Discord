name: 'Build fabric 1.19.x'
run-name: Build fabric-1.19.x-${{ inputs.version }}

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version'
        required: true
        default: '0.0.0-dev'
  workflow_call:
    inputs:
      version:
        required: true
        type: string

jobs:
  build-fabric-1-19-x:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: mc2discord-fabric-1.19.x
    steps:
      - uses: actions/checkout@v3
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Cache Gradle
        uses: actions/cache@v3
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-fabric-1.19.x-${{ runner.os }}-${{ hashFiles('**/gradle.properties', '**/build.gradle.kts', '**/settings.gradle', '**/gradle-wrapper.jar') }}
      - name: Grant execute permission for gradlew
        run: chmod +x gradlew
      - name: Build with Gradle
        run: ./gradlew build
        env:
          INPUT_VERSION: ${{ inputs.version }}
      - uses: actions/upload-artifact@v3
        with:
          name: mc2discord-fabric-1.19.x-${{ inputs.version }}
          path: |
            ${{ github.workspace }}/mc2discord-fabric-1.19.x/build/libs/
            !${{ github.workspace }}/mc2discord-fabric-1.19.x/build/libs/*-all.jar